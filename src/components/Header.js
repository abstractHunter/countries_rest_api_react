import React from 'react'
import {
    HeaderContainer,
    HeaderThemeSwitcher,
    HeaderTitle
} from '../styles/components/header'

function Header() {

    return (
        <HeaderContainer>
            <HeaderTitle>
                where in the world
            </HeaderTitle>
            <HeaderThemeSwitcher>
                sdffsdf
            </HeaderThemeSwitcher>
        </HeaderContainer>
    )
}

export default Header
